version: '3'
services:
  simulator:
    image: federicociner/weather-generator:latest
    volumes:
      - ./output:/home/weather-generator/output
    entrypoint: ["/bin/bash", "/home/weather-generator/run.sh", "${obs}"]

volumes:
  output: